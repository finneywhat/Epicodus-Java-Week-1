apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "event-planning"
version = '1.0'
mainClassName = "EventPlanning"

repositories {
  mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.+'
}
